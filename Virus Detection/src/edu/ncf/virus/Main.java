package edu.ncf.virus;

import java.io.IOException;

public class Main {
	public static void main(String[] args) throws IOException {
		System.out.println("Helo viruzssz! prepair 2 DIE");

		// Sterilizer s = new Sterilizer();
		// s.sterilizeFolder("viruses");

		// s.sterilizeFolder("win_xp_benign");

		// File f = new File("viruses_sterilized");
		// File[] list = f.listFiles();
		// for (File a : list) {
		// if (a.length() == 0)
		// a.delete();
		// }

		// File f = new File("win_xp_benign_sterilized");
		// File[] list = f.listFiles();
		// for (File a : list) {
		// if (a.length() == 0)
		// a.delete();
		// }
	}
}
